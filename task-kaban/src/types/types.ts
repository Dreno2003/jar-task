export interface UserType {
    id?: string,
    fullName: string
}


export interface todotype {
    todos: string;
    completed?: boolean;
    id?:number | any
    // Add other fields with their types
  }